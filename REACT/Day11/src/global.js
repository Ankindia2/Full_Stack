

// usecontext

import { createContext } from "react";

const GlobalContext = createContext("Ankit");

export default GlobalContext;


// first create a create context
// provid data ko usko andar:Descendent
// consume the context

// props use na krna pare.